import { getUpdatedDataHelper } from '../getUpdatedDataHelper';

describe('getUpdatedDataHelper', () => {
  it('should ', () => {
    const expected = getUpdatedDataHelper();
    const result = true;
    expect(expected).toEqual(result);
  });
});
