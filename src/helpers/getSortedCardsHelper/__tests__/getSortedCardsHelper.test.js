import { getSortedCardsHelper } from '../getSortedCardsHelper';

describe('getSortedCardsHelper', () => {
  it('should ', () => {
    const expected = getSortedCardsHelper();
    const result = true;
    expect(expected).toEqual(result);
  });
});
